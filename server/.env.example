PORT=3001
CLIENT_ORIGIN=http://localhost:5173

# Single admin credential - no user accounts, just this one gate
ADMIN_PASSWORD=change-me
JWT_SECRET=change-this-to-a-long-random-string

# Leave as-is to use a local SQLite file
DB_PATH=./leads.db
